@{
    ViewBag.Title = "Konie";
}
@model IEnumerable<HorseTournamentWebPage.Models.HorseModel>
<h2>@ViewBag.Title</h2>
<ul>
    @foreach (var h in Model)
    {
        <li>@Html.ActionLink(h.name + " " +h.year, "HorseResults", "Home")</li>
    }
</ul>